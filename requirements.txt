# KG-Commit reproducibility package -- Python dependencies.
# Python 3.10+ (developed on 3.13). Install:  pip install -r requirements.txt
#
# External (non-pip) prerequisites, NOT installed here:
#   * Neo4j 5.x running at bolt://localhost:7687 (default auth neo4j/password1234;
#     override per-project under config/projects.yaml -> neo4j:).
#   * git on PATH (the build reads cloned repos via `git show`/`git diff`).
#   * A cloned target repo at repos/apache/<project>/ and the ApacheJIT CSVs at
#     data/apachejit/projects/apache_<project>{,_diff}.csv  (read in place).

# --- graph store + git ---
neo4j>=5.0
GitPython>=3.1          # kg_commit.knowledge.dataloader uses `from git import Repo`
tqdm>=4.65              # progress bars during base-graph ingestion

# --- Java parsing (AST / CFG / DFG / PDG / SEQ builders) ---
javalang>=0.13.0

# --- config ---
PyYAML>=6.0             # config/projects.yaml

# --- scientific stack (inference, scalability, statistics) ---
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
networkx>=3.0

# --- figures + notebooks ---
matplotlib>=3.7
nbformat>=5.9           # notebooks are assembled cell-by-cell, then executed
nbconvert>=7.0          # invoked as `python -m nbconvert` / `jupyter nbconvert`
jupyter-core>=5.0       # provides the `jupyter` CLI used by some notebook builders
ipykernel>=6.0          # kernel used to execute the notebooks
ipython>=8.0            # IPython.display inside the generated notebooks
